# small-angular-app
First Clone the project, where you want to save
on phpmyadmin create a database name "ng_project"
Import database file from the database folder(you will find the file database folder)
Run the project on localhost
